{
  "name": "crane-extension",
  "version": "0.1.0",
  "private": true,
  "description": "Crane browser extension — Chrome MV3 client for the Crane OSINT platform.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.300",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2"
  }
}
