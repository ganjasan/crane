# add-browser-extension

Chrome MV3 browser extension for one-click incident capture, coverage gap suggestions, and duplicate detection — backed by 4 new API endpoints and a normalized-URL column on Incident
