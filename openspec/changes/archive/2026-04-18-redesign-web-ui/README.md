# redesign-web-ui

Reskin the web UI into a SaaS-style layout (dark sidebar + light content, monochrome slate palette) with a component-based template architecture on Tailwind standalone CLI, HTMX, and Alpine
